#!/bin/sh
set -e
systemctl daemon-reload || true
if [ "${1:-}" = "remove" ] || [ "${1:-}" = "purge" ]; then
  rm -rf /run/axon-pulse
fi
if [ "${1:-}" = "purge" ]; then
  rm -rf /var/lib/axon-pulse
fi
